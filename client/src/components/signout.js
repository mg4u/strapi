import React from 'react'

export default class SignOut extends React.Component {
    constructor (props){
        super (props)
    }

    render () {
        return (
            <div>Sign Out</div>

        )
    }
}