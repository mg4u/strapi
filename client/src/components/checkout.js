import React from 'react'

export default class CheckOut extends React.Component {
    constructor (props){
        super (props)
    }

    render () {
        return (
            <div>Check Out</div>

        )
    }
}